<UserControl x:Class="AIUnittestExtension.MyToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:theming="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:util="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Utilities"
             xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             xmlns:local="clr-namespace:AIUnittestExtension"
             toolkit:Themes.UseVsTheme="True"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400"
             Name="MyToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <!--Row 0 -->
        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="10,10,5,10">Chat GPT Api Key:</Label>
        <PasswordBox x:Name="APITextBox" Grid.Row="0" Grid.Column="1" Margin="5,10,5,10" VerticalAlignment="Center"/>
        <!--Row 1 -->
        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="10,10,5,10">Model:</Label>
        <TextBox x:Name="ModelTextBox" Grid.Row="1" Grid.Column="1" Margin="5,10,5,10" VerticalAlignment="Center"/>
        <!--Row 2 -->
        <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="10,10,5,10">Select Output Path:</Label>
        <TextBox x:Name="FolderPathTextBox" Grid.Row="2" Grid.Column="1" Margin="5,10,5,10" VerticalAlignment="Center" IsReadOnly="True"/>
        <Button Grid.Row="2" Grid.Column="2" Content="Browse..." Margin="5,10,10,10" Click="BrowseButton_Click"/>
        <!--Row 0 -->
        <Button Content="Generate Unit Test"
         Click="GenerateButton_Click"
         Width="120"
         Height="30"
                Grid.Row="3"
                Grid.Column="1"
         x:Name="GenerateButton" />
        <local:LoadingControl x:Name="LoadingIndicator"  Grid.Row="4" Grid.Column="1"/>
     
    </Grid>
</UserControl>
